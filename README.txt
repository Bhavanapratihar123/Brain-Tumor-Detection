1. Run below command to load all the required libraries.
--> pip install -r requirements.txt

2. Run below command to run server for Flask
--> python app.py

3. Goto the local host URL to access the application.